name: Update frappecli Formula

on:
  repository_dispatch:
    types: [new-release]
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout tap repository
        uses: actions/checkout@v4

      - name: Get latest frappecli release
        id: release
        run: |
          # Get latest release tag
          LATEST_TAG=$(curl -s https://api.github.com/repos/pasogott/frappecli/releases/latest | jq -r '.tag_name')
          echo "latest_tag=$LATEST_TAG" >> $GITHUB_OUTPUT
          
          # Remove 'v' prefix for version number
          VERSION=${LATEST_TAG#v}
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          
          # Get current version from formula
          CURRENT_VERSION=$(grep -oP 'version "\K[0-9.]+' Formula/frappecli.rb || echo "0.0.0")
          echo "current_version=$CURRENT_VERSION" >> $GITHUB_OUTPUT
          
          if [ "$VERSION" != "$CURRENT_VERSION" ]; then
            echo "needs_update=true" >> $GITHUB_OUTPUT
            echo "New version available: $VERSION (current: $CURRENT_VERSION)"
          else
            echo "needs_update=false" >> $GITHUB_OUTPUT
            echo "Formula is up to date"
          fi

      - name: Calculate SHA256
        if: steps.release.outputs.needs_update == 'true'
        id: sha
        run: |
          TAG=${{ steps.release.outputs.latest_tag }}
          SHA256=$(curl -sL "https://github.com/pasogott/frappecli/archive/refs/tags/${TAG}.tar.gz" | shasum -a 256 | cut -d' ' -f1)
          echo "sha256=$SHA256" >> $GITHUB_OUTPUT
          echo "SHA256: $SHA256"

      - name: Update formula
        if: steps.release.outputs.needs_update == 'true'
        run: |
          TAG=${{ steps.release.outputs.latest_tag }}
          VERSION=${{ steps.release.outputs.version }}
          SHA256=${{ steps.sha.outputs.sha256 }}
          
          # Update version and SHA256 in formula
          sed -i "s|archive/refs/tags/v[0-9.]*\.tar\.gz|archive/refs/tags/${TAG}.tar.gz|g" Formula/frappecli.rb
          sed -i "s|sha256 \"[a-f0-9]*\"|sha256 \"${SHA256}\"|g" Formula/frappecli.rb
          
          echo "Updated formula to $TAG"

      - name: Create Pull Request
        if: steps.release.outputs.needs_update == 'true'
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "chore(frappecli): update to ${{ steps.release.outputs.latest_tag }}"
          title: "Update frappecli to ${{ steps.release.outputs.version }}"
          body: |
            Automated update for frappecli formula.
            
            **New version:** `${{ steps.release.outputs.version }}`
            **Previous:** `${{ steps.release.outputs.current_version }}`
            **SHA256:** `${{ steps.sha.outputs.sha256 }}`
            
            **Release notes:** https://github.com/pasogott/frappecli/releases/tag/${{ steps.release.outputs.latest_tag }}
            
            **Changes:** https://github.com/pasogott/frappecli/compare/v${{ steps.release.outputs.current_version }}...${{ steps.release.outputs.latest_tag }}
          branch: update-frappecli-${{ steps.release.outputs.version }}
          delete-branch: true
